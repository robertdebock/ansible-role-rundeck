---
# defaults file for rundeck
rundeck_url: http://localhost:4440

# The Xmx memory size in mb.
rundeck_xmx: 1024
rundeck_xms: 256
rundeck_maxmetaspacesize: 128

# rundeck_serverhttpport: 4440
# rundeck_serverhttpsport: 4443
# rundeck_serverhttphost: 0.0.0.0
# rundeck_serverhostname: "{{ ansible_fqdn }}"
# rundeck_serverwebcontext: "/"
rundeck_rdeckbase: /opt/rundeck
# rundeck_serverdatastorepath: /var/rundeck
# rundeck_defaultusername: admin
# rundeck_defaultuserpassword: admin
# rundeck_rundeckjaaslogin: true/false
# rundeck_loginmodulename: unknown
# rundeck_loginmodule.confname:
# rundeck_rundeckconfigname: /etc/rundeck/rundeck.conf
# rundeck_rundecksslconfig: /etc/rundeck/ssl.conf
# rundeck_rundeckjettyconnectorforwarded: false
# rundeck_rundeckjettyconnectorsslexcludedprotocols: "SSLv3"
# rundeck_rundeckjettyconnectorsslincludedprotocols:
# rundeck_rundeckjettyconnectorsslexcludedciphersuites:
# rundeck_rundeckjettyconnectorsslincludedciphersuites:
