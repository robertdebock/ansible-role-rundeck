---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: rundeck-fedora-rawhide
    image: fedora:rawhide
    registry:
      url: registry.fedoraproject.org
provisioner:
  name: ansible
  inventory:
    host_vars:
      rundeck-fedora-rawhide:
        ansible_python_interpreter: /usr/bin/python3
    group_vars:
      all:
        rundeck_ignore_docker: no
        service_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: fedora-rawhide
